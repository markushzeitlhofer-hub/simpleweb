{
  "name": "simpleweb",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"(no tests yet)\" && exit 0",
    "lint": "echo \"(no linter yet)\" && exit 0"
  },
  "dependencies": {
    "express": "*"
  }
}

